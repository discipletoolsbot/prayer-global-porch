{
  "name": "prayer-global-porch",
  "version": "1.8.4",
  "description": "Prayer Global Porch",
  "main": "prayer-global-porch.php",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "release": "./release.sh",
    "postrelease": "echo 'Pushing new tag to master' && git add . && git commit -m \"chore: Release ${PG_VERSION}\" && git tag ${PG_VERSION} && git push --tags origin master",
    "test": "npx jest -c ./jest.config.js",
    "test:update": "npx jest -c ./jest.config.js -u"
  },
  "repository": {
    "type": "git",
    "url": "git@github.nathinabob:Pray4Movement/prayer-global-porch.git"
  },
  "keywords": [
    "prayer"
  ],
  "author": "Disciple.Tools",
  "license": "GPL-2.0",
  "devDependencies": {
    "@babel/eslint-parser": "^7.19.1",
    "jest": "^29.2.2",
    "jest-image-snapshot": "^6.0.0",
    "jest-puppeteer": "^6.1.1",
    "puppeteer": "^19.2.2",
    "puppeteer-full-page-screenshot": "^1.2.2"
  }
}
